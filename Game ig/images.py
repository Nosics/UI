import pygame as py

BASE_IMAGE_PATH = "images/"

imageList = ["background.png"]

def loadImgs(img):
    for x in imageList:
        py.image.load(BASE_IMAGE_PATH).convert()
        return img
    
